/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plus.unsplash.com','cdn.discordapp.com', 'unsplash.com', 'images.unsplash.com']
    }
}

module.exports = nextConfig
